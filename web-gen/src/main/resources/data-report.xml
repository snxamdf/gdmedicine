<?xml version='1.0' encoding='UTF-8'?>
<dataset>
	<!-- 模块id值规则：report + 两位序列号 -->
	<!-- 模块project_id值规则（支持多项目） ：对应项目id + "," -->
	<t_gen_modules id="report01" code="report" generate="true" name="报表模块" author="sdh" project_id="isy-report,"/>
	
	
	<!-- 表id值规则：对应模块id + 三位序列号 -->
	<!-- 表module_id值规则（支持多模块） ：对应模块id + "," -->
	<!-- 表extend值规则 ：sys(cdm中继承bmain)或tree(cdm中继承btree) -->
	<t_gen_tables id="report01001" code="t_rpt_reportexp" generate="true" menu="false" name="报表公式" comments="报表公式" extend="sys" module_id="report01,"/>
</dataset>
