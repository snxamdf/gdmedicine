<?xml version='1.0' encoding='UTF-8'?>
<dataset>
	<!-- 模块id值规则：channel + 两位序列号 -->
	<!-- 模块project_id值规则（支持多项目） ：对应项目id + "," -->
	<t_gen_modules id="channel01" code="channel" generate="true" name="通道模块" author="yanghongyan" project_id="isy-channel,"/>
	
	
	<!-- 表id值规则：对应模块id + 三位序列号 -->
	<!-- 表module_id值规则（支持多模块） ：对应模块id + "," -->
	<!-- 表extend值规则 ：channel(cdm中继承bmain)或tree(cdm中继承btree) -->
	<t_gen_tables id="channel01001" code="t_chan_smslog" generate="true" menu="true" name="短信日志" comments="短信日志" extend="sys" module_id="channel01,"/>
</dataset>
